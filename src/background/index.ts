// Background service worker for Job Application Co-Pilot
// Handles extension lifecycle and communication between content scripts and UI

import { Logger } from '../utils';

const logger = new Logger('Background');

logger.info('Job Application Co-Pilot background script loaded');

// TODO: Implement mapping engine and extension orchestration
// This will be developed in future phases of the project